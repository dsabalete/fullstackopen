{
  "name": "blog-list",
  "version": "1.0.0",
  "description": "Blog list app - part 4",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "build:ui": "rm -rf build && cd ../../part3/notes-frontend && yarn build --prod && cp -r build ../../part4/blog-list",
    "deploy": "fly deploy --ha=false",
    "deploy:full": "yarn build:ui && yarn deploy",
    "logs:prod": "fly logs",
    "test": "cross-env NODE_ENV=test PORT=3002 jest --verbose --runInBand --watchAll tests/blog_api.test.js",
    "lint": "eslint ."
  },
  "author": "David Sabalete",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.0",
    "mongoose-unique-validator": "^2.0.3"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.4.9",
    "@testing-library/jest-dom": "^5.11.10",
    "@testing-library/react": "^11.2.6",
    "cross-env": "^7.0.3",
    "eslint": "^7.21.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7",
    "supertest": "^6.1.3"
  }
}
